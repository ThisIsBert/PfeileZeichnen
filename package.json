{
  "name": "leaflet-arrow-drawing-tool",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "lint": "node scripts/lint.mjs",
    "format:check": "node scripts/format-check.mjs",
    "validate:imports": "node scripts/validate-imports.mjs",
    "check": "npm run lint && npm run format:check && npm run validate:imports"
  }
}
